/*
// Exportar funções de seta
export const somar = (a, b) => a + b;

export const subtrair = (a, b) => a - b;

export const multiplicar = (a, b) => a * b;

export const dividir = (a, b) => a / b;
*/

/*
// Importar as funções de seta do arquivo "funcoes.js"
import { somar, subtrair, multiplicar, dividir } from "./funcoes.js";

// Usar as funções exportadas
console.log(somar(5, 3)); // Saída: 8
console.log(subtrair(10, 4)); // Saída: 6
console.log(multiplicar(2, 5)); // Saída: 10
console.log(dividir(10, 2)); // Saída: 5
*/